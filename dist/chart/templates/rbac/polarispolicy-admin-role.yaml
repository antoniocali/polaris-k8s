{{- if .Values.rbac.helpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "polaris-k8s.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "polaris-k8s.resourceName" (dict "suffix" "polarispolicy-admin-role" "context" $) }}
rules:
- apiGroups:
  - polaris.k8s.calific.io
  resources:
  - polarispolicies
  verbs:
  - '*'
- apiGroups:
  - polaris.k8s.calific.io
  resources:
  - polarispolicies/status
  verbs:
  - get
{{- end }}
